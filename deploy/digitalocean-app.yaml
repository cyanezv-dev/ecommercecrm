# El app spec para DigitalOcean está en la raíz del repo:
#   .do/app.yaml
#
# Es un STATIC SITE (Vite → dist/). No uses Web Service para este proyecto.
#
# doctl:
#   doctl apps create --spec .do/app.yaml
#   doctl apps update "$DO_APP_ID" --spec .do/app.yaml
